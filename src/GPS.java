/**
 * SYLLABUS: LAB 3 – Multiple Inheritance using Interfaces
 *
 * This is the second interface used to demonstrate multiple interface
 * implementation in a single class (PremiumVehicle).
 *
 * Interfaces define a contract that implementing classes must follow.
 */
public interface GPS {

    /**
     * Returns GPS tracking information for a vehicle.
     *
     * @return GPS details as a String
     */
    String getGPSInfo();
}
